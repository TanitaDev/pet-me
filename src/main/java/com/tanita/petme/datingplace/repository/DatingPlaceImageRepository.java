package com.tanita.petme.datingplace.repository;

import com.tanita.petme.datingplace.model.DatingPlaceImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DatingPlaceImageRepository extends JpaRepository<DatingPlaceImage, Long> {
}
